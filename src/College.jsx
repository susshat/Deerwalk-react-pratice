import React from "react";

const College = ({ name, phoneNumber }) => {
  return (
    <div>
      <p>college is {name}</p>
      <p>phone is {phoneNumber}</p>
    </div>
  );
};

export default College;
